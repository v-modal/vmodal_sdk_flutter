name: vmodal_cctv_example
description: Compile-checked VModal Flutter CCTV upload and search example.
publish_to: none
version: 1.0.0

environment:
  sdk: '>=3.12.0 <4.0.0'
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  vmodal_sdk_flutter:
    path: ../..

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
